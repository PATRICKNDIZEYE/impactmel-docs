# Rendered output: large, regenerable with ./make.sh
out/
out-*/

# Generated placeholder PNGs (regenerated by build.mjs / make-placeholders.mjs)
fixtures/*.png

# Node deps live inside the Docker image, not in the tree
node_modules/

# Saved browser auth state for the screenshot capture harness — never commit
tools/capture/.auth/
